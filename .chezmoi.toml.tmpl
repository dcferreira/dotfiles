{{- $email := promptString "email" "daniel.ferreira.1@gmail.com" -}}
{{- $pyenv := promptBool "using pyenv?" false -}}
{{- $conda := promptBool "using conda?" false -}}
{{- $poetry := promptBool "using poetry?" false -}}
{{- $golang := promptBool "using golang?" false -}}
{{- $rust := promptBool "using rust?" false -}}
{{- $nodenv := promptBool "using nodenv?" false -}}
{{- $rbenv := promptBool "using rbenv?" false -}}
{{- $bentoml := promptBool "using bentoml?" false -}}
{{- $gcloud := promptBool "using gcloud?" false -}}
{{- $azure := promptBool "using azure?" false -}}
{{- $work_cyan := promptBool "is this a cyan machine?" false -}}
[data]
    email = {{ $email | quote }}
    pyenv = {{ $pyenv }}
    conda = {{ $conda }}
    poetry = {{ $poetry }}
    golang = {{ $golang }}
    rust = {{ $rust }}
    nodenv = {{ $nodenv }}
    rbenv = {{ $rbenv }}
    bentoml = {{ $bentoml }}
    gcloud = {{ $gcloud }}
    azure = {{ $azure }}
    work_cyan = {{ $work_cyan }}

